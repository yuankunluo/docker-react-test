sudo: required
services: 
  - docker

before_install:
  - docker build -t yuankunluo/docker-react-test -f Dockerfile.dev .

script:
  - docker run -e CI=true yuankunluo/docker-react-test npm run test -- --coverage